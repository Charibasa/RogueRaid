using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class JY_TeleporterExit : MonoBehaviour
{
    public int floor;
    public bool hasBeenAccessed;

    // Start is called before the first frame update
    void Start()
    {
        hasBeenAccessed = false;
    }
}
